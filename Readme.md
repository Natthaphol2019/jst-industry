# JST Industry – Docker Setup Guide
วิธีติดตั้งและรันโปรเจกต์สำหรับผู้ร่วมทีม

โปรเจกต์นี้ใช้ **Docker** เพื่อให้ติดตั้งง่าย ทุกคนรันได้เหมือนกัน 100%  
เพียงมี Git + Docker ก็เริ่มงานได้ทันที ไม่ต้องลง XAMPP หรือ PHP เพิ่ม

---

## ✅ 1. เตรียมความพร้อม
ให้ติดตั้งโปรแกรมต่อไปนี้บนเครื่องของคุณ:

1. **Git**  
2. **Docker Desktop**  
   - เปิด Docker Desktop แล้วรอจนขึ้นสถานะ:  
     **"Docker Desktop is running"**

---

## ✅ 2. ดาวน์โหลดโปรเจกต์จาก Git
เปิด Terminal (Git Bash / CMD / PowerShell) แล้วรัน:

```
git clone https://github.com/<your-name>/<repo-name>.git
cd jst-industry
```

> ❗ เปลี่ยน `<your-name>/<repo-name>` ให้เป็นชื่อจริงของโปรเจกต์

---

## ✅ 3. รัน Docker (PHP + Nginx + MySQL)
ในโฟลเดอร์โปรเจกต์ ให้รันคำสั่ง:

```
docker-compose up -d --build
```

เช็กว่า container รันครบ:

```
docker ps
```

ควรเห็น:

- php-app  
- php-nginx  
- jst-mysql  

---

## ✅ 4. นำเข้า Database
ในโฟลเดอร์นี้มีไฟล์ `jst_industry.sql` ไว้ให้แล้ว  
รันคำสั่ง:

```
docker exec -i jst-mysql mysql -u root -proot jst_industry < jst_industry.sql
```

ต้องไม่ขึ้น error ใด ๆ

---

## ✅ 5. เปิดใช้งานเว็บไซต์
เปิด Browser แล้วเข้า:

```
http://localhost:8000
```

คุณจะเจอหน้า Login ของระบบ

---

## 🔐 บัญชีสำหรับเข้าใช้งานครั้งแรก
```
Username: admin
Password: 123456
```
(สามารถเปลี่ยนได้หลังจากเข้าใช้งาน)

---

## 🔧 คำสั่งที่จำเป็นสำหรับผู้พัฒนา
หยุด Docker ทั้งหมด:

```
docker-compose down
```

สตาร์ทใหม่:

```
docker-compose up -d
```

ดู container ที่กำลังรัน:

```
docker ps
```

ดู log ของ PHP:

```
docker logs php-app
```

ดู log ของ MySQL:

```
docker logs jst-mysql
```

---

## 🎉 เสร็จเรียบร้อย!
ตอนนี้คุณสามารถพัฒนาโปรเจกต์นี้ได้เหมือนกันทุกคน  
ถ้ามีปัญหาเรื่อง Docker, Database หรือ Path ให้ทักผู้ดูแลโปรเจกต์ได้ทันที

